<!DOCTYPE html>
<html>
    <head>
        {# ... #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            {{ encore_entry_link_tags('main_styles') }}
        {% endblock %}
        {# ... #}
    </head>
    <body>
        {# ... #}
    </body>
</html>