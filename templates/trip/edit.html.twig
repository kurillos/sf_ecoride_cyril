{% extends 'base.html.twig' %}

{% block title %}Modifier le covoiturage{% endblock %}

{% block body %}
    <div class="container my-5">
        <h1 class="text-center mb-4">Modifier le covoiturage</h1>

        {{ form_start(tripForm) }}
            {{ form_widget(tripForm) }}
            <button type="submit" class="btn btn-success mt-3">Enregistrer les modifications</button>
        {{ form_end(tripForm) }}
    </div>
{% endblock %}
