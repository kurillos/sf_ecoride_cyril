{% extends 'base.html.twig' %}

{% block body %}
    <main>
        <section class="text-center py-5">
            <div class="container">
                <h1 class="display-4 fw-bold text-primary">EcoRide : Covoiturer pour un monde plus propre.</h1>
            </div>
        </section>

        <section class="text-center text-white d-flex align-items-center justify-content-center py-5">
            <div class="container">
                <div class="search-box mx-auto mt-5">
                    <form action="{{ path('app_trip_search') }}" method="get">
                        <div class="row g-3 justify-content-center">
                            <div class="col-md-3 col-sm-6 input-group mb-3">
                                <span class="input-group-text"><i class="bi bi-geo-alt-fill"></i></span>
                                <input type="text" class="form-control" id="departure" name="departure" placeholder="Ville de départ" required>
                            </div>
                            <div class="col-md-3 col-sm-6 input-group mb-3">
                                <span class="input-group-text"><i class="bi bi-geo-alt-fill"></i></span>
                                <input type="text" class="form-control" id="destination" name="destination" placeholder="Ville d'arrivée" required>
                            </div>
                            <div class="col-md-3 col-sm-6 input-group mb-3">
                                <span class="input-group-text"><i class="bi bi-calendar-date"></i></span>
                                <input type="date" class="form-control" id="date" name="date" placeholder="Date de départ" required>
                            </div>
                            <div class="col-md-3 col-sm-6 input-group mb-3">
                                <span class="input-group-text"><i class="bi bi-person"></i></span>
                                <input type="number" class="form-control" id="passengers" name="passengers" placeholder="1 passager" required min="1">
                            </div>
                        </div>

                        <div class="row mt-3 justify-content-center">
                            <div class="col-auto">
                                <button type="submit" class="btn btn-success text-white fw-bold">Rechercher</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </section>

        <section class="text-center">
                <p>Bienvenue chez EcoRide - la nouvelle plateforme française qui transforme vos trajets en actes écolos !</br>
                Notre mission ? Réinventer le covoiturage en voiture pour réduire l'empreinte carbonne de nos déplacements, tout en vous faisant faire des économies.
                </p>
                <img class="rounded" src="{{ asset('build/images/covoiturage.png') }}" alt="covoiturage">
                <p>Moins de voitures sur la route, moins de pollution, plus de rencontres et de trajets partagés : c'est ça l'esprit EcoRide.</br>
                Notre ambition est grande : devenir la référence du covoiturage écologique en France et au-delà, pour tous les voyageurs soucieux de leur impact et prêts à changer les habitudes.</br>
                <strong>Ensemble, on va plus loin. Rejoignez l'aventure EcoRide dès aujourd'hui !</strong>
                </p>
                <img class="rounded" src="{{ asset('build/images/energie_propre.png') }}" alt="energie_propre">
        </section>

           <section class="py-5">
    <div class="container">
        <div class="row align-items-start">
            
            {# BLOC "TRAJETS LES PLUS FRÉQUENTS" #}
            <div class="col-md-6">
                <div class="travel d-flex flex-column align-items-center">
                    <h2 class="text-success">Trajets les plus fréquents</h2>
                    <div class="row">
                        {% for trip in latest_trips %}
                            <div class="col-md-6 mb-4">
                                <a href="{{ path('app_trip_show', {'id': trip.id}) }}" class="card-link">
                                    <div class="card">
                                        <div class="card-body">
                                            <h5 class="card-title text-success">{{ trip.departureLocation }} &rarr; {{ trip.destinationLocation }}</h5>
                                            <p class="card-text">Départ: {{ trip.departureTime|date('d/m/Y H:i') }}</p>
                                            <p class="card-text">Prix: {{ trip.pricePerSeat }} crédits</p>
                                        </div>
                                    </div>
                                </a>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
            
            {# BLOC "LES SERVICES À BORD" #}
            <div class="col-md-6">
                <div class="service d-flex flex-column align-items-center"> 
                    <h2 class="text-success">Les services à bord</h2>
                    
                    <div class="d-flex flex-column"> 
                        <div class="d-flex align-items-center mx-2 mb-2"> 
                            <i class="bi bi-usb-symbol text-success me-1"></i>
                            <p class="mb-0">Prise USB</p>
                        </div>
                        <div class="d-flex align-items-center mx-2 mb-2">
                            <i class="bi bi-wifi text-success me-1"></i>
                            <p class="mb-0">Wi-fi</p>
                        </div>
                        <div class="d-flex align-items-center mx-2 mb-2">
                            <i class="bi bi-music-note-list text-success me-1"></i>
                            <p class="mb-0">Musique</p>
                        </div>
                        <div class="d-flex align-items-center mx-2 mb-2">
                            <i class="bi bi-snow3 text-success me-1"></i>
                            <p class="mb-0">Climatisation</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
    </main>
{% endblock %}